#include "headers/Board.h"
#include "headers/Square.h"

Board::Board() {
    for(int r = 0; r < MAX_ROWS; r++) {
        for (int c = 0; c < MAX_COLUMNS; c++) {
            // TODO
        }
    }
}

Board::~Board() {}